# 23. 베타 운영 기준 (지인 베타 → 재발급 → 정식 판매 결정) — 2026-07-11 운영자 확정

> 작성 2026-07-11. 전제 상태(실측): Q7 전체 완결 + 월 감사 2026-07 GREEN(생존 변이 0), 기준선
> **831 passed / 4 skipped**, main `9c8720d`. 이 문서는 "게이트가 못 보는 것"(문안 체감·실사용 만족)을
> 실사용자로 측정하는 마지막 검증층의 운영 규칙이다.

## 1. 목적과 성공 기준

- 목적: 정식 판매 전에 (a) 발송 파이프라인 전 구간을 실주문으로 리허설하고, (b) 도구가 측정 못 하는
  품질(직답성 체감·사람 상담가 느낌)을 실사용자 피드백으로 측정한다.
- 종료(성공) 기준: 베타 N건 발송 완료 + hsweep Z=0 달성 + 피드백 백로그 트리아지 완료 → 운영자가
  정식 판매 개시를 결정한다. (N은 운영자 결정 — 제안 기본값 3~5건.)

## 2. 발송 전 체크리스트 (매 건, 순서 고정)

1. 표준 게이트 파이프라인 전용 생성(손편집 HTML/PDF 금지 — QI-2026-06-26-01) → `gate_pass=True`.
2. **hsweep 파일럿 계측**(감사 A-3, docs/16 2026-07-06 절): 첫 베타 발송물이 그 "검수 전 신선 발송물"이다.
   후보 N → 생존 M → 확정 K, 운영자 신규 발견 Z 기록(목표 Z=0, 정밀도 K/M ≥ 0.7). 실행은 3중 잠금 +
   운영자 승인.
3. 운영자 육안 검수: `handoff/templates/pdf_review_report.md` 형식 + 금칙 스캔(실명·적중·AI 언급) +
   300dpi 시각 점검.
4. APPROVED 전이 후 **수동 발송**(자동 발송 없음 — 절대규칙 16).

## 3. 알려진 제약 (베타 안내·운영 숙지)

| 제약 | 내용 | 근거 |
|---|---|---|
| 무LLM 폴백 분량 | N≥3 모듈 조합은 LLM-on 전제(장애 시 해당 주문 대기) — 무LLM 발급은 N≤2만 | 실렌더 경계표 2026-07-11 |
| 동명 given 커플 | 성 제외 이름이 같은 2인 주문은 접수 차단(호칭 구분 불가) | 라운드14 |
| 시진 불명 | integrated_full은 접수 차단. 개인 상품은 정오 추정 + 고지(절대규칙 8) | 3-A |
| 후속 질문 | integrated_full 부모 주문의 후속(텍스트·PDF)은 미지원(차단) | 3-A |
| veraPDF | 7.1-3 clause 1건 잔존(측정만, 빌드 불차단) | render.md |

## 4. 수집 지표와 피드백 처리

- 정량: hsweep N/M/K/Z, 운영자 육안 신규 발견 수, 생성~발송 소요 시간, LLM 비용/건.
- 정성(지인 피드백 축 4개): ① 질문에 정면으로 답했는가 ② 사람 상담가의 글로 읽히는가
  ③ 분량·구성 체감 ④ 거슬리는 표현/오탈자.
- 처리 규칙: 결함 = docs/16 QI 등재(증상/영향/원인 2층/재발방지) / 개선 희망 = 백로그 등재 후 베타
  종료 시 일괄 트리아지(개별 즉시 구현 금지 — 완성 우선 결정 2026-07-11) / 게이트 사각 = 양방 테스트
  동반 수정(규율 3).

## 5. PII·고지 (베타에도 동일 적용)

- 지인도 고객 규율 동일: 익명 ID로만 기록, 실명·생년월일·질문 원문의 채팅/커밋/장부 전재 금지,
  산출물은 ignored 영역만. 베타 전 **A-2(ignored 55파일 정리) 선행 권장** — 신규 산출물이 기존 미정리
  더미에 섞이지 않게.
- 본문 무고지(절대규칙 18). 베타 안내·판매 페이지 등 대외 채널은 감수 명시형("자동 분석 도구로
  산출하고 운영자가 직접 검수·감수한 자료"). 예측 정확도 주장 전면 금지.

## 6. 재발급 (기존 고객)

- 대상·시점 = 운영자 결정. 재발급은 저장 content.json 재렌더(무과금) 우선, 문안 세대가 크게 바뀐
  경우만 재compose(과금 — 운영자 승인).
- 재발급본도 §2 체크리스트 전 항목 적용(구버전과의 육안 비교 1회 포함 — 회귀 체감 확인).

## 운영자 확정 사항 (2026-07-11)

1. 베타 인원 **N=3**(지인, 명단은 문서 비기록 — PII. DB 별칭으로만 관리) — §1 종료 기준의 N도 3.
2. 가격 = **무료**(베타 고지 시 감수 명시형 문구 사용, §5).
3. 재발급 = **기존 고객 1인 선행**(운영자 지정 — 별칭으로 관리, 이 문서 비기록). 베타 3건과 별도 트랙,
   §6 규칙 적용(저장본 세대 확인 → 재렌더/재compose 판단).
