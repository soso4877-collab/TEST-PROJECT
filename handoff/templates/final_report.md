# 최종 보고 — <작업명>

1. 생성/수정 파일 목록:
2. 전체 tests PASS 수:
3. hpreflight 결과(preflight_ok, calc_diff_empty, secret_hit_count):
4. hverify_pdf 결과(개인/궁합 gate_pass, hit counts):
5. hrun(no-regen) 결과(all_gates_pass 또는 missing_pdf):
6. summary 리포트 경로:
7. LLM 호출 0 확인:
8. PDF 재생성 0 확인:
9. git status --short:
10. git diff --stat HEAD:
11. calc/ diff 없음:
12. `.env`·render/out·local 프로파일·reports 추적 0:
13. 커밋 안 함 확인:
14. push 안 함 확인:

> 검증 실패 시: "완료" 단정 금지. 어느 PDF·페이지·문구가 남았는지 위치만 보고하고 중단(자동 수정 금지).
