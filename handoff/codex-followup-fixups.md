# [Codex 수정 라운드 지시문] 후속·재방문 패킷 — 잔여 항목 정리 (재발주 아님)

> 형식: 승인된 TASK_PACKET(산문판). 신선 컨텍스트에서 이 문서만으로 실행 가능.
> 근거 SSOT: `AI-Brain/50_Decisions/2026-07-07-sajugen-출시판정.md`(E4~E13) · vault `75_Content-Domain/상담가-페르소나-메타발화-금지.md`.
> repo: `C:\Users\pc\test-project`. 구현 = Codex. 검증 = 별도 Claude 세션 /cross-review. 커밋 = 운영자.

## 상태 (이 지시문의 전제)
- **T0①②③ + T1~T4 = Codex 구현 완료**(샌드박스 660 passed/31 skipped, 미커밋). 교차리뷰 라운드1~3 진행됨(`REVIEW-FEEDBACK.md`).
- **이 지시문은 재발주가 아니라 남은 3항목의 수정 라운드**다. T0①②③~T4 코드는 다시 만들지 말 것.
- **기준선 = 기준환경(전 리소스) 654 passed / 4 skipped**(E3 확정). Codex 샌드박스는 리소스 부재로 skip 상이(31) — 완료 근거는 기준환경 수치로만.

## 0. 역할·금지 (매 항목 준수)
- **Codex 상시 금지**: git commit·push·deploy·LLM 호출·PDF 재생성. 각 항목 완료 시 pytest 전체 실행 + diff·증거 보고까지, 커밋은 운영자.
- **불변 제약**: 3단 가드(safe_lint→factcheck→trace) 우회·완화 금지(사각 축소 방향만). `calc/`·`input/` 무변경(문안·문서만) → 골든 22건 불변. PII 0(합성·익명). 한국어.
- 범위 = 아래 A·B·C **세 항목으로 한정**. 동결 규칙(E12) — 그 밖 개선·리팩터링 금지, 발견은 기록만.

---

## ① REVIEW-FEEDBACK.md 미해결 항목 전수 정리 (각 항목 → 처분)

| 출처 | 항목 | 처분 |
|---|---|---|
| 라운드2 ④ | T0-④ 메타발화(면책 선언형·의료 회피형) 미구현 | → **항목 A**(아래) |
| 라운드3 관찰 | `content.md:12` 문서-코드 정합 갭("문구 고정") | → **항목 B**(아래) |
| 라운드2 관찰 | `followup/compose.py` 범위밖 가드 edge(저장 `allowed_years` 빈 경우·명시연도 없는 "내년"+연도지평 stale) | → **항목 C**(아래, 경계 명시) |
| 라운드3 ③ | 연애 완화형 `rules.py:1659`·`1668` | **해소** — 운영자 비준: 유지 확정(긍정 유도 문맥형 hedge, 면책 낭독 아님). 코드 변경 없음. |
| 라운드2 커밋분리안 | `HANDOFF.md`(세션시작 M + Codex분 혼재)·무관 선행분 | **운영자 액션**(커밋 시 `git add -p` 분리) — Codex 코드 대상 아님. |

→ 이 라운드에서 Codex가 손대는 것은 **A·B·C 뿐**.

---

## 항목 A — T0-④ 상담가 페르소나 메타발화 제거 (면책 선언형 + 의료 회피형)

> 스펙 정본: vault `75_Content-Domain/상담가-페르소나-메타발화-금지.md`. 원칙 한 줄: **풀이문은 상담가의 말 — 문서·법무팀 말이 새면 페르소나가 깨진다.**

**메타발화 3종 중 이번 대상 = 2종**(상담 유도형은 T0-①에서 완료 → "상담에서" grep 0건 재확인만):
- **면책 선언형**: "보장 금지 = 보장 표현의 부재이지, 비보장의 *선언*이 아니다." 상담가는 면책 조항을 낭독하지 않는다.
- **의료 회피형**: 장 도입·마무리에 고정 삽입되는 "병원에서 확인/전문가와 상의" 보일러플레이트.

**대상 앵커(vault 기준 — 라인은 T0로 밀리므로 반드시 패턴 grep 전수)**: `content/rules.py:1240`(합격/취업 결과 면책)·`1257`(수익/손실 면책)·`1266`(병원 진료 도입 보일러)·`1285`(병원 확인 마무리 보일러). + 스윕(`단정하지는 않\|단정하거나 보장\|병원\|진료\|전문가와 상의`)으로 계열 잔존(예: 월별장 "결과지에서는…단정하지 않습니다") 발견 시 **동일 처분**. 프롬프트측 `content/llm_sections.py`의 "병원에서 확인/전문가와 상의" 유도 문구도 제거(질병 단정 절대 금지는 유지).

**판별 기준(핵심 — 스펙 정본)**:
- **보일러플레이트(제거)**: 고객 사연과 무관하게 템플릿 위치에 반복 삽입되는 지시·선언.
- **문맥형(유지)**: 그 고객 사연에서 자연히 나온 배려·프레이밍. **유지 확정**: `rules.py:775`(지역 설명 속 "병원과 장보기"), "병을 진단하는 자리가 아니라…살피는 자리입니다"(장 성격 프레이밍).
- **경계(유지 비준됨)**: `1659`·`1668` 연애 완화형 — 손대지 말 것.
- 판별 질문: "사람 상담가가 이 고객에게, 이 자리에서 이 말을 하겠는가?"

**하드 린트 금지**(방법론 B-8): 보일러 vs 문맥형 구분이 필요해 정규식 하드밴은 고오탐 → **좁은 회귀 테스트만**.

**수용 기준**: (제거측) 확정 보일러 문자열이 룰 산출에 재등장 0 + (유지측) 프레이밍·지역 문장 존재(과삭제 방지) + 해당 챕터 `safe_lint`·`guarantee_lint` clean 유지 — **양방**(방법론 A-3).
**동반 테스트**: `tests/test_metadiscourse.py`(합성·PII 0). 기존 `tests/test_p3.py`의 "병원 포함" 기대값은 새 스펙에 맞게 갱신하되 **의료 단정어 가드(병에 걸린·불치·사망·단명 등)는 불변**.

---

## 항목 B — content.md:12 의료 규칙 두 층 분리 (문서-코드 정합)

**대상**: `.claude/rules/content.md:12` (단 한 줄, 문서 전용 — 코드/테스트 변경 금지).
**현재 원문**:
```
- health 섹션: 의료 단정 절대 금지, "의료 전문가 상의" 문구 고정. 투자·법률 단정 금지.
```
**두 층 분리(정확한 문구는 Codex가 자연스럽게, 의미 보존)**:
- ① **유지(불변 안전)**: "의료 단정 절대 금지 … 투자·법률 단정 금지" — 보장 표현 부재 원칙과 동축, 절대 삭제 금지.
- ② **폐지→대체**: "'의료 전문가 상의' 문구 고정"(고정 문구 강제 = 보일러 원흉)을 교체 → "기계적 회피 문구(병원 확인·전문가 상의 등)를 템플릿 위치(장 도입·마무리)에 고정 삽입 금지. 의료적 배려는 고객 사연에서 자연히 나올 때만(문맥형). 판별 기준·실사례 정본 = vault `75_Content-Domain/상담가-페르소나-메타발화-금지.md`."

**스윕 범위(실측 완료 — 확대 금지)**: "문구 고정/필수" stale 패턴은 **content.md:12 1건뿐**. `docs/10-admin-review-workflow.md:29` "윤달/시진불명 고지 문구 삽입"은 **범위 밖·유지**(절대규칙 5 법정 필수 고지, 의료 보일러 아님).

**수용 기준**: 갱신 후에도 content.md에 **"의료 단정" 금지 규칙 생존**(유실=실패) + `"의료 전문가 상의" 문구 고정` 문자열 grep 0건 + 대체 규칙에 vault 경로 포인터 포함 + 코드/테스트 diff 0.

---

## 항목 C — compose.py 범위밖 가드 경계 명시 (라운드2 관찰 종결)

**대상**: `sajugen/followup/compose.py`.
**관찰(라운드2)**: `out_of_scope`가 `allowed_years` 빈 경우 스킵(가드 `allowed_years and …`) → factcheck 백스톱 의존. 또 질문에 명시 연도 없는 "내년" 상대표현 + 리포트 연도지평 통째 stale일 때 anchor=현재연도로 진행.
**처분(최소·비대상 확대 금지)**: 새 거부 로직 추가가 아니라 **경계를 명시적으로 고정**한다 —
- `allowed_years`가 비면 out_of_scope 판정 불가임을 **주석 + 테스트로 명문화**하고, 그 경우 factcheck(`check_with_allow`)가 저장연도 밖 발화를 차단하는지 **회귀 테스트 1건**으로 백스톱 실증(fail-closed 확인).
- 명시 연도 없는 상대표현("내년")은 현행 유지(factcheck 백스톱). 이를 "알려진 경계"로 `tests/test_followup_compose.py`에 주석·케이스로 남긴다.

**수용 기준**: `allowed_years` 빈 저장본 + 저장연도 밖 간지/연도 포함 답변 후보 → 게이트 차단 실증(양방). 기존 범위밖 거부 테스트 불변.

---

## 검증 (완료 근거 — 절대규칙 19)

1. **필수(Codex 실행)**: `./.venv/Scripts/python.exe -m pytest tests/ -q` → exit 0 + passed 수. **기준선 654/4(기준환경) 대비 신규 테스트만큼 증가, 감소 0**(회귀 = passed 감소).
2. **골든 불변**: `./.venv/Scripts/python.exe -m pytest tests/ -q -k golden` → 골든 GREEN(문안·문서만 수정, 계산 무변경).
3. **패턴 검증**: 면책·의료 보일러 grep(content, 산출물·pycache 제외) → 제거확정 0건 / 유지확정(775·프레이밍) 잔존. content.md "문구 고정" 0건.

**완료 보고 형식**(각 항목): 실행 명령 + 출력(passed/exit) + diff 요약(파일·처분) + 경계 사례 근거 → 운영자. 커밋은 운영자. Claude 신선 컨텍스트 /cross-review.

**커밋 단위(운영자용 제안)**: 항목 A(rules.py·llm_sections.py + test_metadiscourse·test_p3, 골든 불변 확인) → 항목 B(content.md 문서) → 항목 C(compose.py + test_followup_compose). T0-④ 계열이라 A는 기존 T0 커밋과 합류 가능.

## 범위 밖 — 손대지 않음 (동결 E12)
T0①②③~T4 코드 재작업(완료분)·연애 완화형 1659·1668(유지 비준)·신규 하드 게이트·verify.py 기하·재상담권 유료화·리팩터링·성능 최적화·궁합 로직. QI 발견 시 기록만.
