## dead-param 스캐너(scripts/deadparam_scan.py, Phase 2 C2) allowlist.
## 형식: <path>::<func>::<param>  # 사유(필수 — 빈 사유는 미등재 취급).
## '##' 로 시작하는 줄은 주석. 각 항목은 '전수 분류'에서 실결함이 아니라고 판정된
## 의도적 미사용이며, 사유는 반드시 참이어야 한다(거짓 사유 = C1/C4 '거짓말하는 문서' 모드).
## 신규 등재 시: 소비처 부재를 실측 확인하고 사유에 근거를 남긴다.

## 관계 폴백 3종: LLM 출력 반려 시의 결정론 대체 텍스트라 고객 맥락(situation)을 반영하지
## 않는 것이 설계(폴백이 고객 상황을 되읊으면 안 됨). 같은 경로의 LLM compose
## (gunghap._compose:999 프롬프트 주입)·질문축(relationship/context._question_axes:170)이
## situation 을 실제 소비 → 시그니처 대칭은 실재(팬텀 아님, QI-2026-07-04-01 재발 아님).
sajugen/gunghap.py::_relationship_slot::situation  # 폴백 결정론 생성, LLM 경로가 situation 소비(대칭)
sajugen/gunghap.py::_relationship_fallback::situation  # 폴백 결정론 생성, LLM 경로가 situation 소비(대칭)
sajugen/relationship/fallback.py::build_fallback::situation  # 폴백 결정론 생성, LLM 경로가 situation 소비(대칭)

## compose(title): title 은 섹션 처리 인터페이스 파라미터로 polish(AnthropicBackend.polish→
## llm_polish.polish(rule_text, title))가 실제 소비한다. compose 는 section_id→_COMPOSE_GUIDE
## 로 프롬프트를 구성해 title 미사용이나, 두 백엔드(Anthropic/Rule) 메서드 시그니처 일관성
## 유지. 제거 시 2 백엔드+2 builder 호출+13개 테스트 파일 churn(반나절 범위 밖).
sajugen/content/llm_sections.py::compose::title  # 섹션 인터페이스 인자(polish 가 소비), compose 만 미사용

